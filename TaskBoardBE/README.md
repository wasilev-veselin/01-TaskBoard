# TaskBoardBE

Minimal Express API

## Run

```bash
npm install
npm run dev
```

Default URL:

```text
http://localhost:3000
```

## Endpoints

- `GET /tasks`
- `GET /tasks/:id`
- `POST /tasks`
- `PATCH /tasks/:id`
- `PUT /tasks/:id`
- `DELETE /tasks/:id`

